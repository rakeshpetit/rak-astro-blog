---
import Header from "../../components/header.astro";
import Footer from "../../components/footer.astro";
import "../../styles/tailwind.css";

import { getCollection } from "astro:content";

const shorts = await getCollection("shorts");
---

<html lang="en" class="h-full">
  <Header />
  <body class="h-full">
    <main class="relative min-h-almost py-8 bg-stone-100">
      <ul>
        {
          shorts.map((short) => {
            return (
              <li class="mb-4">
                <div class="flex justify-center">
                  <div class="w-2/3 rounded-lg bg-white p-6 px-12 shadow-lg">
                    <h2 class="mb-2 text-xl font-extrabold leading-tight text-teal-600 lg:text-3xl">
                      <a href={`/shorts/${short.slug}`}>{short.data.title}</a>
                    </h2>
                  </div>
                </div>
              </li>
            );
          })
        }
      </ul>
    </main>
    <Footer />
  </body>
</html>
