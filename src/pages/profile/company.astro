---
import { getEntry } from "astro:content";

const oracle = await getEntry("profile", "oracle");
const { Content: Oracle } = await oracle.render();
---

<article
  class="prose prose-pink prose-headings:font-bold lg:text-2xl lg:leading-relaxed pb-4"
>
  <Oracle />
</article>
